package HelloWorld.java;

public class MergeSort {
	private static void mergeSort(int[] input Array) {
		int inputLenght = inputArray.length;
		if (inputLength < 2) {
			return;
			
		}
		int midIndex = inputLength /2;
		int[] leftHalf = new int[midIndex];
		int[] rightHalf = new int{inputLength - midIndex];
		
		for(int i = 0; inputLength)}
		}
		for (int i = bubbe.length -1 i>=0 ;i--) {
			for(int j=0; j<i;j++) {
				if(bubble[j] > bubble[j=1]) {
					int temp=bubble[j];
					bubble[j] = bubble[j+1];
					bubble[j=1]= temp;
				}
			}
		}
	}
	
	public static void main(String[] args) {
		int[] bubble = {88,15,45,63,27};
		bubbleSort(bubble ) {
			for (int num:bubble) {
		System.out.println("num + " ");"
	  }
	}
	
}
